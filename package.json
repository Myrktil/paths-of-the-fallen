{
  "devDependencies": {
    "esbuild": "^0.25.9",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "build": "node build.js --sourcemap --local-files --banners",
    "build-clean": "node build.js --clean --sourcemap --local-files --banners",
    "build-rc": "node build.js --sourcemap --local-files --banners --reset-config",
    "publish": "node build.js --clean --reset-config --user-files"
  }
}
